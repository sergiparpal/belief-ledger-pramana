# belief-ledger-core

Host-neutral deterministic core for evidence-backed policy enforcement for AI agents. It contains
domain models, events, SQLite store/migrations/projections, reasoning engine, normalized ingestion,
gating, linting, verification, context selection/rendering, structured-model budgets, lifecycle
contracts, capability profiles, bound action decisions, and audit primitives. It imports and runs
without Hermes installed.
